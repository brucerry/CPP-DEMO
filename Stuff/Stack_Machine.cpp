#include <iostream>
#include <string>
#include <stack>
using namespace std;

int main() {
  int n;
  string cmd;

  cin >> n;

  while (n--) {
    cin >> cmd;

    if (cmd == "PUSH") {
      
    }

    else if (cmd == "DUP") {
      
    }

    else if (cmd == "UNION") {
      
    }

    else if (cmd == "INTERSECT") {
      
    }

    else {

    }

    if (stack.size()) {
      cout << stack.top().size() << endl;
    }
  }
}